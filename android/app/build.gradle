android {
    namespace = "com.example.drivox"
    compileSdk = 35 // ⬅️ Explicitly set to 35
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.drivox"
        minSdk = 21 // ⬅️ Set this according to your needs (21 is common)
        targetSdk = 35 // ⬅️ Explicitly set to 35
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}
